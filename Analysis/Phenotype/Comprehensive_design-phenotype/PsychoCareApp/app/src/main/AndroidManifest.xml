<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 사진 읽기 권한 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Android 12 이하 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- ACCESS_MEDIA_LOCATION: GPS EXIF 데이터 읽기 (시간대 알고리즘에 필요) -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- 피노타입 분석 권한 -->
    <!-- 통화 기록: 런타임 요청 가능한 일반 위험 권한 -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- 저장된 연락처 이름 읽기 (통화 기록의 cached_name 보완) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- 앱 사용 통계: 일반 런타임 요청 불가 — 설정 화면 유도 필요 -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".PsychoCareApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.PsychoCare">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Gemma 모델 파일 경로 -->
        <meta-data
            android:name="com.google.mediapipe.ASSET_DIR"
            android:value="models" />

    </application>
</manifest>
