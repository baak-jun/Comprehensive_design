# Codex Startup Notes

When starting work in this project, read `docs/CODEX_STARTUP.md` first.

Always check today's date before editing or building. The daily progress note and debug APK copy name must use the current date, and the note must record the current `versionName`.
