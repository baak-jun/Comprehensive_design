# 2026-05-30 작업 기록

## 현재 상태

- 현재 앱 버전: `versionName = "0.4.14"`
- 직전 진행 문서: `docs/2026-05-29-progress.md`
- 직전 빌드 설정 APK 이름: `app/build/outputs/apk/debug/Counseling_05_29_v0.4.9_debug.apk`
- 오늘 빌드 설정 APK 이름: `app/build/outputs/apk/debug/Counseling_05_30_v0.4.14_debug.apk`
- 오늘 시작 시점의 git 작업트리는 깨끗했다.
- 오늘은 기능 변경 없이 날짜/진행 문서 정리부터 시작하므로 앱 버전은 올리지 않는다.

## 현재 구현된 기능 요약

- LiteRT-LM 기반 `.litertlm` 모델 로드
- Gemma 4 E4B IT 상담 응답 스트리밍
- Android 음성 인식 기반 텍스트 입력
- 앱 내 16kHz mono PCM WAV 즉시 녹음 후 오디오 입력
- 이미지 첨부 경로는 네이티브 크래시 위험 때문에 직접 모델 전달을 중단하고 텍스트 fallback 중심으로 정리
- Health Connect 주/월 요약 화면과 상담 프롬프트 주입
- 마지막 상담 세션 자동 저장/복원
- 여러 상담 세션 저장/전환
- 중요 기억 저장/삭제
- SQLite FTS 기반 RAG-lite 관련 과거 문맥 주입
- 설정 탭/설정 다이얼로그, 테마, 글씨 크기, 사고 모드
- checkpoint-11500 LoRA 병합 및 LiteRT 변환 준비 문서/스크립트

## 오늘 우선순위

1. 날짜 기준 문서와 APK 이름 정리
   - `docs/2026-05-30-progress.md` 생성
   - `copyDatedDebugApk` 산출물 이름을 `Counseling_05_30_v0.4.9_debug.apk`로 변경
   - 기능 변경이 없으므로 `versionName = "0.4.9"` 유지

2. 빌드 검증
   - Android Studio 내장 JBR 사용
   - 명령:
     ```powershell
     $env:JAVA_HOME='C:\Program Files\Android\Android Studio\jbr'
     $env:Path="$env:JAVA_HOME\bin;$env:Path"
     .\gradlew.bat :app:assembleDebug
     ```

3. 실제 기기 테스트
   - 모델 로드
   - 기본 텍스트 상담 응답
   - 앱 내 즉시 녹음 WAV 입력 후 응답
   - Android 음성 인식 텍스트 입력
   - 앱 재시작 후 마지막 활성 세션 복원
   - 세션 목록/새 세션/세션 전환
   - 중요 기억 저장/삭제
   - RAG-lite 관련 과거 문맥 주입
   - Health Connect 권한, 데이터 표시, 상담 프롬프트 주입

4. 안정화 후보
   - Health Connect 포함 토글과 기간 선택 상태 저장 여부 결정
   - 세션 이름 변경/삭제 UI 검토
   - 중요 기억 직접 추가/수정 UI 검토
   - RAG-lite 검색어 추출, user 메시지 가중치, 날짜/중요 기억 가중치 개선
   - `MainActivity.kt`가 2500줄 이상이므로 기능별 파일 분리 검토

## 오늘 처리한 내용

- 진행 문서 생성:
  - `docs/2026-05-30-progress.md`
- 빌드 산출물 날짜 정리:
  - `Counseling_05_29_v0.4.9_debug.apk`
  - `Counseling_05_30_v0.4.9_debug.apk`
- 모델 제외 누락 파일 확인:
  - Gradle 프로젝트 로드는 성공했다.
  - `assembleDebug`는 `app/src/main/cpp/llama.cpp`가 없어 CMake 설정에서 실패했다.
  - 현재 앱 실행 경로는 `LiteRtLmCounselingEngine`이며 `NativeCounselingEngine`/GGUF fallback은 호출되지 않는다.
  - 모델을 제외하면 필요한 민감정보 파일은 발견되지 않았다.
  - LiteRT-LM 중심 빌드를 위해 Gradle의 `externalNativeBuild`/NDK 설정을 제거해 `llama.cpp` 없이 빌드되도록 정리했다.
- 빌드 검증:
  - `.\gradlew.bat :app:assembleDebug` 성공
  - 생성된 날짜 APK: `app/build/outputs/apk/debug/Counseling_05_30_v0.4.9_debug.apk`
- 1차 리팩토링:
  - `MainActivity.kt`를 2501줄에서 1470줄로 축소했다.
  - 앱 화면/상태 모델을 `AppModels.kt`로 분리했다.
  - 갤러리 화면을 `GalleryScreen.kt`, 갤러리 권한/조회 로직을 `GalleryData.kt`로 분리했다.
  - Health Connect 화면을 `HealthScreen.kt`, 데이터 읽기 로직을 `HealthData.kt`로 분리했다.
  - 채팅 첨부 파일/녹음/WAV 처리 로직을 `ChatInputFiles.kt`로 분리했다.
  - 세션 JSON 내보내기/불러오기를 `ChatSessionIo.kt`로 분리했다.
  - 프롬프트/기억/RAG-lite 문맥/사고 모드/마크다운 유틸을 `ChatPromptUtils.kt`로 분리했다.
  - 리팩토링 후 `.\gradlew.bat :app:compileDebugKotlin` 성공
  - 리팩토링 후 `.\gradlew.bat :app:assembleDebug` 성공
- 2차 리팩토링:
  - 채팅 화면 묶음을 `ChatScreen.kt`로 분리했다.
  - 채팅 말풍선/상태 표시/마크다운 표시를 `ChatMessageComponents.kt`로 분리했다.
  - 기억/세션/설정 다이얼로그를 `ChatDialogs.kt`로 분리했다.
  - 입력바와 첨부/녹음/말하기 메뉴를 `ChatInputBar.kt`로 분리했다.
  - `MainActivity.kt`는 앱 시작과 상단/하단 내비게이션 중심의 183줄 파일로 축소했다.
  - 현재 가장 큰 파일은 상태/이벤트 흐름을 가진 `ChatScreen.kt` 736줄이다.
  - 2차 리팩토링 후 `.\gradlew.bat :app:compileDebugKotlin` 성공
  - 2차 리팩토링 후 `.\gradlew.bat :app:assembleDebug` 성공
- 테스트 피드백 반영:
  - 설정 다이얼로그 내용을 `LazyColumn`으로 바꿔 작은 화면에서도 테마 버튼까지 스크롤해서 볼 수 있게 했다.
  - 오디오만 첨부하고 텍스트 없이 전송할 때 기본 문구를 "사용자의 음성 입력"으로 바꿨다.
  - LiteRT-LM에 `Content.AudioFile(...)`을 보낼 때도 음성 첨부를 사용자 발화로 간주하라는 안내를 텍스트 컨텍스트에 함께 넣도록 했다.
  - 채팅 목록을 아래로 아주 짧게 스크롤해도 상단 UI가 바로 접히도록 아래 방향 스크롤 감지 임계값을 낮췄다.
  - 키보드 표시 시 `adjustResize`와 Compose IME 패딩이 중복되어 생기던 입력창 아래 여백을 줄이기 위해 직접 IME 하단 패딩 적용을 제거했다.
- 버전 정리:
  - 테스트 피드백으로 사용자 동작이 바뀌었으므로 `versionName`을 `0.4.10`으로 올렸다.
  - 날짜 APK 이름을 `Counseling_05_30_v0.4.10_debug.apk`로 변경했다.
- 0.4.11 추가 처리:
  - PhenoType 모듈을 `src/PhenoType` 소스셋으로 앱 빌드에 포함했다.
  - 하단 탭에 `패턴` 화면을 추가했다.
  - `패턴` 화면에서 통화 기록 분석과 앱 사용 통계 분석을 볼 수 있게 했다.
  - 통화 기록 분석에는 `READ_CALL_LOG` 런타임 권한을 사용한다.
  - 앱 사용 통계 분석에는 `PACKAGE_USAGE_STATS` 권한을 사용하며, Android 사용 정보 접근 설정 화면으로 이동하도록 했다.
  - 키보드 표시 중에는 하단 탭 바를 숨기고, 입력바에 `imePadding()`을 적용하는 방식으로 여백 처리를 다시 조정했다.
  - 기능 변경이므로 `versionName`을 `0.4.11`로 올렸다.
  - 날짜 APK 이름을 `Counseling_05_30_v0.4.11_debug.apk`로 변경했다.
- 0.4.12 추가 처리:
  - `패턴` 화면의 `상위 앱` 문구를 `앱별 사용 시간 - 최근 7일 상위 N개`로 명확히 바꿨다.
  - 앱 사용 통계 목록을 최근 7일 합산 1분 이상 앱 중 사용시간 순 최대 30개까지 표시하도록 늘렸다.
  - 상담 설정에 `패턴 포함/패턴 제외` 토글을 추가했다.
  - `패턴 포함`이 켜져 있으면 메시지 전송 시 통화 패턴과 앱 사용 패턴을 읽어 마지막 사용자 프롬프트에 `[생활 패턴 요약]`으로 주입한다.
  - 생활 패턴 요약은 진단/감시가 아니라 수면, 회피, 고립, 과사용 가능성을 조심스럽게 확인하는 보조 맥락으로만 쓰도록 지시한다.
  - 기능 변경이므로 `versionName`을 `0.4.12`로 올렸다.
  - 날짜 APK 이름을 `Counseling_05_30_v0.4.12_debug.apk`로 변경했다.
- 0.4.13 추가 처리:
  - 설정 창을 접이식 섹션으로 정리했다.
    - 기본으로 `모델`, `응답 옵션`만 열고, `대화 관리`, `표시`, `테마`는 필요할 때 펼치도록 했다.
  - 탭 전환 시 `ChatScreen`이 composition에서 제거되며 LiteRT-LM 엔진이 release되는 문제를 줄이기 위해, 채팅 화면은 유지하고 갤러리/건강/패턴 화면을 위에 덮는 방식으로 바꿨다.
  - 앱 시작/채팅 진입 시 자동 모델 로드를 중단했다.
    - 모델은 사용자가 `모델 파일 선택`을 눌렀을 때만 로드한다.
    - 발열/메모리 급증과 탭 전환 후 재로드 가능성을 줄이기 위한 조치다.
  - 앱 사용통계 집계를 `queryUsageStats` 값만 쓰지 않고 `UsageEvents` foreground/background 이벤트 기반 계산과 병합하도록 보강했다.
    - YouTube/게임처럼 기존 목록에서 누락될 수 있는 앱을 더 잘 잡기 위한 변경이다.
  - 기능/안정화 변경이므로 `versionName`을 `0.4.13`으로 올렸다.
  - 날짜 APK 이름을 `Counseling_05_30_v0.4.13_debug.apk`로 변경했다.
- 설정 UI 후속 정리:
  - `모델`, `표시`, `테마`는 접이식에서 빼고 바로 보이도록 했다.
  - 접이식은 `대화 관리`, `응답 옵션` 중심으로 남겼다.
  - 접이식 헤더는 채워진 버튼 색상으로, 내부 옵션은 outline 버튼과 선택 상태 색상으로 구분해 계층이 더 보이도록 조정했다.
  - UI 변경이므로 `versionName`을 `0.4.14`로 올렸다.
  - 날짜 APK 이름을 `Counseling_05_30_v0.4.14_debug.apk`로 변경했다.

## 다음에 할 일

1. 실제 기기에서 0.4.9 기능 묶음 테스트
2. 테스트 결과에 따라 0.4.10 안정화 작업으로 버전 올릴지 결정
3. 필요하면 `ChatScreen.kt`의 상태/이벤트 흐름을 별도 state holder로 추가 분리

## 0.4.15 model output guard

- Added a LiteRT-LM short-reply guard. If streaming returns only a 1-2 character/symbol response, the app recreates the conversation once and retries with a Korean complete-sentence instruction.
- Reduced phenotype prompt app list injection from 12 apps to 6 apps to keep the prompt lighter.
- Updated `versionName` to `0.4.15`.
- Updated dated APK name to `Counseling_05_30_v0.4.15_debug.apk`.

## 1.0.0 GitHub build baseline

- Set GitHub/build baseline app version to `1.0.0`.
- Updated dated APK name to `Counseling_05_30_v1.0.0_debug.apk`.
- Added GitHub Actions debug APK workflow at `.github/workflows/android-debug.yml`.
